# memory/

세션에서 수거한 **암묵지와 반복 패턴**.

## 상태 모델 (중요)
- **후보(candidate):** memory-tick이 자동 저장한 메모. '검증 대기' 상태. 즉시 지식이 아니다.
- **지식(knowledge):** 사용자가 승인한 뒤, 승격 기준을 통과해 `principles/`·`decisions/`·`personas/` 등으로 승격된 메모.
- 규칙: 자동 저장은 '후보'로만 들어온다. AI 초안·자동 캡처는 사용자 승인 전까지 지식이 아니다 (`ai-dev-신념.md` §7 작성모델과 정합).

## 자동 저장 — memory-tick (운영 규칙)
저장은 모델의 자율 판단(빌트인)에만 맡기지 않는다. **stop-hook 기반 강제 발화**로 저장 판단을 트리거한다.

- **메커니즘:** `stop-hook-throttle.sh`가 일정 경과마다 훅 메시지로 찔러주면, tick 스킬이 해당 구간을 돌아보고 저장 여부를 판단.
- **트리거 시점:** 텔레그램 reply 후, 사용자와 직접 대화한 턴 이후.
- **명시 트리거:** "기억해 / 저장해줘" 요청에도 활성화. 명시 호출 불필요.
- **저장소 일원화:** `.claude/settings.local.json`의 `automemorydirectory`가 Obsidian 경로를 가리키도록 설정 → 빌트인(자율)과 tick(stop 훅 강제, 독립 실행) 두 경로 모두 같은 저장소로.
- **빌트인 vs tick:** 빌트인=시스템 프롬프트 자율 판단 지침, tick=stop 훅 강제 발화 독립 장치. 기억은 모델의 선의에만 맡기지 않는다.

## 실패·예외 시나리오
- **hook 미작동 / tick 누락:** 정기 점검 — 세션 종료 전 후보 메모가 있는지 수동 확인.
- **중복 저장:** 같은 사실은 새 것으로 갱신(overwrite), 누적하지 않는다.
- **저장 실패:** 다음 훅/세션에서 재시도. 실패 사실 자체도 후보로 기록.
- **승인 대기 누적:** 후보가 일정량 쌓이면 사용자에게 일괄 검토 요청.

## 승격 기준
세션을 주기적으로 모아 맥락을 비우고, **학습 가치가 있는 조각**을 승격.
- 반복 패턴(2회 이상 등장) → 후보 → 승인 → 지식.
- 1회성 사실은 메모로만, 승격 금지.
휘발 프롬프트 → machine-readable 지식. 세션은 끝나도 학습은 남는다.

## 참조 (영감/패턴 출처)
- `principles/references.md` — grill-me, mattpocock/skills, ouroboros, oh my codex/openagent, Mem0, seCall
