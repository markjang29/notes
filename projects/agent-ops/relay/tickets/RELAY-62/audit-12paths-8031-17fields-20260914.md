---
title: 감사 보고 — 8024 통합 감사·영향성검토 12건 + 8031·17필드 판본 검증
date: 2026-09-14
status: submitted (controller 검수 대기)
tags:
  - audit
  - aws-audit
  - 8024
  - approval-link
  - canon
---

# 감사 보고 — 8024 통합 감사·영향성검토 12건 + 8031·17필드 판본 검증

- 담당: `aws-audit` (감사 CODEX) · read_only · state_transition·self_approval 없음
- 정본: notes origin/main `0ce7e83` 기준 실측 (2026-09-14)
- 의존: 매니저 [의견조의] 8024 승인링크시스템(7단계) — 3단계(컨펌)·5단계(매니저 리뷰) 감사 관여 예정

## 1. 배정 12건 경로 전수 실측

| 배정 경로 | 8018 (Flask) | 8024 (Spring) | 근거 구현 |
|---|---|---|---|
| /reports | 200 (보고함) | 200 (portal.html) | 양쪽 병존 — 8018 원본·8024 투영 |
| /tests | 200 | 404 | 8018 테스트결과 (FULL TEST 원장 판독) |
| /tests/<slug> | 404 | — | 미존재 |
| /control/flow | 404 | 200 (flow.html) | 8024 관리면 |
| /control/agents | 404 | 404 | 페이지 미구축 — 실체는 8024 `/api/topology/public`의 렌즈 `agents` (bot·actor·agent·worker 4kind) |
| /approve | 404 | 404 | 실체: 8018 `/api/dossier-approve` (401 fail-closed) + 8005 `/api/decide/{item_id}` |
| /dossiers·/novel-plan·/fleet·/sites·/roadmap | 200 | — | PAGE_IDS 등록 실측 (matrix-home `main.py:81-85`) |

관리면 보안(8024): `TopologyManagementBoundaryFilter` — 로컬한정 + `X-Topology-Admin-Token`
+ 쓰기토글(env 기본 false) + 본문 2MB 한도. `/api/topology/public` 200 무인증(읽기),
관리 API 403 fail-closed. 8005: `/health` 200, 판정 API 401 (cookie `ab_token`).

## 2. 판본 차이 검증 2건

### 2-1. 8031 (아카 수집 현황) — 8018 누락

- 8024 `sites.json` 27사이트 중 8031 등록 (owner: firebat-zcode(01)) — 확인.
- 8018 `/api/sites`도 **27건 중 8031 포함** (route_ready·up·functional 전부 true, `EXTRA_SITE_ROWS.ARCA_ROW`, `main.py:1142`).
- 단, 8018 정적 HTML(`/sites`, `/` 8.3KB)에는 8031 문자열 0건 — **클라이언트 fetch('/api/sites') 렌더링이므로** 8018 `/api/components`(5건)에도 8031 없음.
- **정정(전회 보고 대비)**: "8018 목록에 없음"→"API엔 있음, 정적 카탈로그·components에는 미등록". 누락이 아니라 **등록층 3중(TOP 렌즈/SITES/EXTRA_SITE_ROWS) 분산**이 원인. 승인링크시스템 착수 전 단일 원장(8024)→투영 구조로 층(phase) 통합 권고.

### 2-2. RELAY-49 "17필드" vs 실측 16필드

- 정본: `matrix-human-approval-v1.schema.json` required **16** (props 16, additionalProperties false).
- 08-03 초판(c8808d7)부터 required 16 — **제정신 이후 1필드 빠진 적 없음(원래 16)**.
- RELAY-49 `spec.md:26` "승인 17필드" 표기만 1개 과다.
- 스키마 16: approval_id·artifact_kind·artifact_id·artifact_revision·artifact_sha256·generation_projection_sha256·normalization_bundle_sha256·rights_status·decision·approver_actor·actor_registry_ref·approved_at·transport_receipt_sha256·materialized_by_actor·authentication·contract.
- **판정**: 정본(schema)이 16이고 지고, RELAY-49 spec 주석이 1개 과다. 3단계(컨펌) 착수 전
  매니저→zcode(SE)가 spec.md:26 문구를 "16필드"로 정정 커밋할 것 권고 (감사는 본문 수정 안함).

## 3. 승인링크시스템(7단계) 감사 준비물

- 리뷰 체크리스트 3줄: ①계약정합(16필드·SHA·인증) ②멱등성·락·보안(중복승인 0·로컬+토큰+fail-closed 3중) ③React best practices(async-parallel·bundle 분리·rerender 최소화).
- 검증 시나리오 3건: S1 승인flow(레코드 1건·16필드 검증·sites.json↔/dossiers 정합) / S2 반려flow(append-only·재승인시 신규 레코드) / S3 감지폴링(지연 ≤2폴링·stale판본 표시·CERT 불일치 자동반려).

## 4. 결론

- 배정 12건 중 실존 경로 6, 미구축 3(/approve·/control/agents·/tests/<slug>), 나머지 관리·API면.
- 8031·17필드 판본 차이 2건 모두 검증 완료 — 8031은 "미등록 아님·정적HTML 미노출", 17필드는 "spec 주석 1과다(정본=16)".
- controller 검수 후 3단계(컨펌)·5단계(매니저 리뷰) 진입 시 시나리오 S1~S3 실시.
