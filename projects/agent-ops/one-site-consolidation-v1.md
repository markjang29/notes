---
title: 원사이트 통합 투트랙 v1 — 8GB 정리 전제
date: 2026-09-06
status: 이사님 지시 확정 — 진행 관리 중(매니저)
tags: [agent-ops, one-site, spring]
---

# 원사이트 통합 투트랙 v1

> 이사님 09-06 지시: "원사이트로 합치는거 빨리 — 8GB 정리 전제. 스프링 부트로. 홈에 덕지덕지
> 연결된 걸 모바일·PC(추후 앱)에서 쓰게 디자인 다시 뽑아라. 레거시 정합성과 신규 정합성 투트랙."
> 매니저 역할: 현황 조사·진행률 관리·분배(구현은 위치별 봇).

## 목표

- 흩어진 포트 사이트들(현재 16+)을 **Spring Boot 원사이트 하나**로 통합 — matrix-studio-spring
  (8024, React 동서빙 실측됨)이 그 뼈대. React shell 반응형(모바일 퍼스트 + PC), 추후 앱 = 같은 API 재사용.
- 완성되면 AWS 8GB 셧다운(엣지 512MB + 홈으로 최종 정리).

## 트랙 A — 레거시 정합성 체크 (홈에 이미 배포된 것)

- 대상: 홈 GM 리눅스 8018 MATRIX 홈·8021 스튜디오·8022 넥서스 + 허브 사이트맵이 가리키는 전 링크.
- 체크: 각 링크가 ①AWS 공인 IP 의존(8GB 셧다운 시 사망) ②홈 조직망 로컬 가능 ③죽은 링크 —
  3분류 목록화. 담당: **gmwin claude**(GM 윈도우, 홈망+공인망 둘 다 도달 가능).
- 산출: 링크 정합성 목록 → notes `projects/agent-ops/legacy-link-audit-2026-09.md`.

## 트랙 B — 신규 원사이트 (스프링 부트)

- 화면 구조(IA) 초안 — 매니저 작성, zcode(SE 정본)가 상세 설계로 발전:
  - 경로 기반 통합(포트 난립 제거): `/`(홈) · `/game`(RPG·아케이드) · `/studio`(스튜디오·넥서스) ·
    `/assets`(시나리오·이미지·워크벤치) · `/daily`(뮤비 일상) · `/control`(회의방·대시보드 — 이사님 전용) · `/approve`(승인).
  - 반응형 원칙: 모바일 퍼스트 카드형, PC 그리드. PWA 준비(앱 = 동일 API).
  - 반응형/성능 기준: react-best-practices 스킬 + AGENTS.md 압축 인덱스(설계 §3).
  - 데이터: provenance 6필드 표준 연계(firebat 초안 진행 중).
- 구현: **firebat**(matrix-studio-spring 소유) — 모듈화(Modulith)와 함께 기능 단위 흡수.
- 검수: 감사(codex×claude 교차).

## 진행률 관리 (이사님 09-06 규칙 반영)

- **보고 형식: "n/20"** — 아래 20장 카드 기준 분수로만 보고.
- **단계 게이트**: 각 단계 완료 → 매니저 검증 → **이사님 확인** → 다음 단계 안건 제시 → 승인 후 진행.
  확인 없이 다음 단계로 가지 않는다.
- **React 작업은 react-best-practices 스킬(Vercel) 필수 적용** — 설계·구현·검수 전 단계. AGENTS.md
  압축 인덱스 방식 병행(Vercel 실측: 스킬 호출 79% < 인덱스 100%).
- 단일 원장: `roadmap.json` stage-1. 봇 보고=회의방 → 매니저 검증 → 원장 갱신 → ELI5 보고.

## 페이지별 담당 봇 매칭 (제안 — 이사님 확인 대기, 09-06)

- `/` 홈 허브 → **zcode** (통합 홈 허브 원제작자, 8018 오너십 계승)
- `/game` (RPG·아케이드) → **rpg 팀**(heav_lnx_rpg_bot) 주관 + 아케이드 콘트롤 협력
- `/studio` (스튜디오·넥서스) → 도메인 **zcode**, 스프링 구현 **firebat claude**(8024 repo 소유)
- `/assets` (자산·시나리오·이미지·워크벤치) → **firewin**(heav_firebat_zcode_bot — 이사님 09-06
  신설 롤 "자산창고 담당"+자산 현황 웹 오너쉽 인수와 정확히 대응). 시나리오 도메인은 scenario 팀장 협력.
- `/daily` (뮤비 일상) → **codex_dev_1** (RELAY-42 music_video 담당 계승)
- `/control` (회의방·대시보드 — 이사님 전용) → **매니저**(관제 오너) + 화면 구현 zcode
- `/approve` (승인보드) → **매니저** (작업요청 창구 오너)
- 공통: UI 설계 zcode · 스프링 구현 firebat claude · 검수 audit(codex×claude) · 반응형 기준
  react-best-practices 스킬 필수.
- 확정 시: roster.json·회의방 공지로 각 봇에게 페이지 오너십 부여, 20장 카드 담당과 연동.

## 20장 카드 (진행률 분모)

- [설계 1~5] 1 IA 상세(zcode·진행중) 2 레거시 링크 감사(gmwin·진행중) 3 반응형 와이어프레임
  4 포트→경로 매핑표 5 provenance 6필드 초안(firebat·진행중)
- [구현 6~16] 6 스프링 Modulith 뼈대 7 React shell 세팅(스킬 적용) 8 홈 `/` 9 `/game` 10 `/studio`
  11 `/assets` 12 `/daily` 13 `/control`(이사님 전용) 14 `/approve` 15 인증·권한 16 PWA 기본
- [검수·이관 17~20] 17 정합성 체크표 수렴(트랙A×트랙B) 18 감사 검수(codex×claude) 19 엣지 Caddy
  공개 중계 20 8GB 셧다운 전 최종 점검

- **현재: 0/20 완료 · 진행 중 3**(카드 1·2·5 — 봇 작업 대기/수행 중). 다음 게이트: 카드 1·2 도착 시
  매니저 검증 → 이사님 확인 요청.
