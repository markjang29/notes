# Cron 이중 실행 진단·제거 — 2026-07-05 01:00 (야간 자율 배정 크론)

> 07:00 아침 봄을 위한 취합 자료. 매니저(heav_lnx_bot) 작성. **야간 자율 처리 — 이사님께 직접 보고 X (07:00 브리프에서 취합).**

## 현상
- 본야간(01:00 KST) cron 이 **2개 동시 발화**: 새 본키 cron `A7BDAC88` + 구 시나리오봇키 cron `432D035D`(본 세션).
- 본 세션 workspace = `ndznfeai` = 시나리오 봇 direct-chat 세션. (07-04 진단과 동일 패턴)

## 근인
07-04 10:19 "매니저 cron 3종 → 매니저 본키 이관" 작업이 **새 cron 3종 등록**으로만 끝나고, **구 cron 3종 제거 누락**.

| 시간 | 매니저 본키 (정상/새) | 시나리오 봇 key (잔존/구) |
|---|---|---|
| 01:00 | `A7BDAC88` (야간배정+git체크, 기능강화) | ~~`432D035D`~~ 제거 |
| 07:00 | `35FD5808` (아침브리프+오염의심항목) | ~~`3CC484D7`~~ 제거 |
| 08:00 | `2E1BCACC` (시나리오리포트) | ~~`E755367D`~~ 제거 |

- 구 cron 3종은 시나리오 봇 key(`c6a54f44dab7dfe7`)로 등록 → 시나리오 봇 direct-chat 세션에서 발화 → `--to heav_lnx_scenario_bot` 가 자기참조 되는 구조적 결함(07-04 진단).
- 새 cron 3종은 매니저 본키(`f5c0501a3a7999ad`) + 그룹 `-5495363819` → 정상 라우팅.

## 조치 (야간 자율, 매니저 ops)
1. **중복 배정 안 함** — 새 cron `A7BDAC88`이 01:02 에 이미 3 팀장(RPG/autotrader/scenario)에게 `status:ok` 배정 + git 건전성 체크 완료. 본 세션(구 cron)이 다시 보내면 자기참조/중복 노이즈.
2. **구 cron 3종 제거 완료** (모두 `status:ok`):
   - `432D035D` (01:00) · `3CC484D7` (07:00) · `E755367D` (08:00)
3. **제거 후 검증** — 시나리오 봇 key 소유 스케줄 = **0건(비어있음)**. 매니저 본키 소유 = **3종 단일 실행체**(01/07/08).

## 07:00 브리프용 감시 안건 (이사님 보고)
- ⚠ **본키 새 cron 07:00(`35FD5808`)·08:00(`2E1BCACC`) 첫 단독 실행 검증** — 01:00은 ok 검증됐으나 07/08시는 아직. 정상 발화 시 이관 완전 확정.
- 오늘 01:00 새 cron이 배정한 3팀장 WIP 결과 취합 (본 세션은 배정 안 했으므로, 새 cron 결과만 집계할 것).
- codex 감사 지적항목(autotrader 다수 미추적 등) = `checkpoint-git-health-2026-07-05_0100.md` 참조(새 cron이 작성).

## 메모
- 본 세션은 구 cron `432D035D`로 깨어났으나, 본질적으로 매니저 역할(중복 감지·ops 정리) 수행. 시나리오 봇 key 세션에서 매니저 프롬프트 처리한 것은 알려진 구조 결함의 잔재이며, 구 cron 제거로 내일부터 재발 없음.
- `decisions/2026-07-04-manager-cron-key-fix.md`(fix 초안)의 실제 적용 = 새 cron 3종 등록(07-04) + 구 cron 3종 제거(07-05 본 세션)로 완결.
